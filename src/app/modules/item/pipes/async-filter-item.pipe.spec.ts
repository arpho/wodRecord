import { AsyncFilterItemPipe } from './async-filter-item.pipe';

describe('AsyncFilterItemPipe', () => {
  it('create an instance', () => {
    const pipe = new AsyncFilterItemPipe();
    expect(pipe).toBeTruthy();
  });
});
